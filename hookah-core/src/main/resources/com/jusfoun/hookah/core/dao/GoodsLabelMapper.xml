<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jusfoun.hookah.core.dao.GoodsLabelMapper">
    <sql id="generateKey">
        select replace(uuid(),'-','')
    </sql>
    <sql id="nowDate">
        select NOW()
    </sql>
    <!-- 批量插入 -->
    <insert id="insertBatch" parameterType="java.util.List">
        insert into goods_label
        (
        lab_id, lab_name, lab_type, lab_py, lab_all_py, user_id, update_time
        )
        values
        <foreach collection="list" index="index" item="item" separator=",">
            (
            (<include refid="generateKey" />), #{item.labName,jdbcType=VARCHAR},
            #{item.labType,jdbcType=VARCHAR}, #{item.labPy,jdbcType=VARCHAR}, #{item.labAllPy,jdbcType=VARCHAR},
            #{item.userId,jdbcType=VARCHAR},(<include refid="nowDate" />)
            )
        </foreach>
    </insert>
</mapper>